```{include} ./bevy-learning-roadmap.md
:parser: myst_parser.sphinx_
```

## 阅读方式

本站沿单一学习序列组织内容：

- 每个编号章节的 README 先解释 Bevy 模型、API 与实验协议，再紧接着展示同目录的 `Rust_in_Bevy.md`。
- 阶段实验不占用课程文章编号，紧跟在所属阶段的最后一个章节之后。
- 最终 2D 垂直切片保留在仓库根目录；其源码由文档站发布到 `capstone/`。

这种组织保留 Bevy 与 Rust 的一一对应关系，又避免每章在站点导航中出现两次。