# Rust in Bevy：阶段 06 集成——发布前的失败契约

垂直切片的完成标准不是「我的机器能运行」。它要在资源缺失、旧存档和首次启动这些真实边界上保留可理解的行为。

## 本阶段唯一主题

为外部 I/O 写出失败契约：输入经过解析和验证，失败被传播或降级，核心规则不直接依赖文件系统。

- 配置与存档格式带版本，迁移和拒绝未知版本都有明确分支。
- 写入采用临时文件与替换策略，并在失败时保留上一份有效数据。
- release 验收在干净环境执行，记录平台、资源路径和已知限制。

验收：模拟不可写目录与无效存档；应用不崩溃、不损坏旧数据，并向玩家说明下一步。
