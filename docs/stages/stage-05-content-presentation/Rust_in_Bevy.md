# Rust in Bevy：阶段 05 集成——表现层适配器

资产、动画、声音和 UI 都在解释游戏状态；它们不应反向定义规则。阶段实验检验这条依赖方向是否守住。

## 本阶段唯一主题

把表现层实现为状态到输出的适配器，并为异步资源保留未就绪分支。

- 游戏规则发出稳定的领域事实，表现系统转换成文本、帧、声音和样式。
- `Handle<T>` 存在不代表资产可用；加载状态决定何时创建表现对象。
- 连续观察同一状态时，输出必须幂等，避免重复播放或重复生成 UI。

验收：故意延迟资产加载，并连续触发一次命中；界面应正常等待，声音和闪烁各出现一次。
