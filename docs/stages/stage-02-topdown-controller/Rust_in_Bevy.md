# Rust in Bevy：阶段 02 集成——输入快照

设备状态会随渲染帧变化，模拟按固定步运行。阶段实验的关键是保存一份可供模拟消费的意图快照。

## 本阶段唯一主题

在 `Update` 采样输入，在 `FixedUpdate` 消费类型化移动意图。

- 快照应是 `Vec2`、动作枚举或专用组件，不能直接把键位判断复制进物理系统。
- 按下边沿与持续轴值分开保存；两者的生命周期不同。
- 暂停时明确决定快照是清空、冻结还是继续更新。

验收：在不同帧率下保持相同方向输入，比较固定步后的位置；暂停后不得出现积压输入突然移动。
