# Rust in Bevy：阶段 03 集成——转换链的可逆性

战术地图的点选正确性来自一条可检查的转换链，不来自「看起来点中了」。窗口、Viewport、相机和世界空间必须逐段可见。

## 本阶段唯一主题

将坐标转换封装为小函数，并用正向、反向标记验证其近似可逆。

- 输入边界处理 `Option`/`Result`；规则层只接收世界坐标。
- 调试 Gizmos 同时画原始点、投影点和回算点。
- 相机缩放或窗口大小变化后，旧缓存不能继续假定有效。

验收：限制 Viewport 并改变缩放，点选标记仍与鼠标对应；失败转换应安全忽略而非崩溃。
