# Rust in Bevy：阶段 04 集成——规则数据流

Arena 原型的复杂度来自规则交错，不来自敌人数量。将每次交互写成数据流，才能看出伤害为何发生、何时死亡。

## 本阶段唯一主题

让碰撞产生命中事实，让规则链归约状态，让表现层订阅结果。

- 几何判定保持纯函数；ECS 系统负责枚举候选实体。
- Message 在同一帧连接检测、结算和反馈，`Health` 保存最终状态。
- 需要顺序的规则使用最小 `.chain()`；独立系统保留并行空间。

验收：替换精灵或关闭音频后，伤害结果与死亡时机保持不变。
