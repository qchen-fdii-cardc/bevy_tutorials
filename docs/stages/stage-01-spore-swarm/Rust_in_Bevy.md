# Rust in Bevy：阶段 01 集成——生命周期管线

孢子群的价值不在实体数量，而在生命周期是否是一条可追踪的管线。生成、移动、衰减、清理分别拥有一个阶段。

## 本阶段唯一主题

用组件的存在与移除表达生命周期阶段，并让 Commands 只承担结构变更。

- `Lifetime` 归实体所有；群体总数或生成规则才属于 Resource。
- 读取寿命、决定死亡、执行 `despawn` 要有可解释的先后关系。
- 生成实体后不要在同一遍历中假设它已被 Query 看见。

验收：将寿命设为零，确认实体只清理一次，且统计系统不会保留悬空的 `Entity` id。
