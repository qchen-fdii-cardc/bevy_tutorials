# Bevy 学习总路线：从「会拼 API」到能设计游戏系统

「跟着教程把方块画出来」不叫学会 Bevy；那只是证明你的复制粘贴链路还活着。

## 省流版

- Bevy 的学习主线只有一条：用 **ECS（Entity, Component, System）** 描述状态，用调度器决定状态如何演化，用渲染、输入和音频把演化暴露给玩家。
- 不要把 Bevy 当成 Unity 的 Rust 皮肤。它的核心难点是 **数据导向设计（Data-Oriented Design）**、查询冲突和调度顺序；先建立模型，API 才会停止随机变脸。
- 每一章都必须交付一个可运行实验、一个可观察的调试手段、一个能被改坏再修复的故障案例。只看、不改、不测，留下的是熟悉感，不是可提取的技能。
- 以 Bevy `0.19` 为当前基线。版本升级时，文章要单列「迁移记录」，严禁把旧版本的 `add_startup_system`、`WindowDescriptor` 等 API 偷渡进新代码。
- 最终项目应是一个小而完整的 2D 游戏垂直切片：移动、碰撞、敌人、UI、音效、存档、场景状态和可发布构建。做「开放世界 MMO」只会制造一个目录结构很大的烂尾工程。

## 这条路线到底在训练什么

很多 Bevy 教程把学习者训练成了 `commands.spawn` 的人肉补全器。代码能跑，问题一换就断电：为什么查询参数冲突？为什么系统先后顺序改变了结果？为什么相机坐标和窗口坐标对不上？为什么一段逻辑放进 `Update` 后帧率一高就发疯？

这些问题没有「多看几个项目」式的神秘解法。它们都指向同一个能力：**将游戏描述为一组可查询的数据，以及对数据变换的受控调度**。

Bevy 的 `Entity` 是身份，`Component` 是事实，`Resource` 是全局或共享状态，`System` 是变换规则，`Schedule` 和 `SystemSet` 则是时间与依赖关系。渲染窗口、精灵、UI、物理、资产加载看上去彼此很远，落到底层，依然是在给实体添加事实、查询事实、修改事实。

> 学 Bevy 时，最值得反复追问的句子是：**这份状态属于谁？谁能读取它？谁能修改它？它在什么时候修改？**
>
> 四个问题回答不清，继续堆 API 只会让类型系统替你保存混乱。

## 开始前的环境基线

本系列固定使用 Rust stable、Cargo 与 Bevy `0.19.x`。每篇文章的仓库都要提供 `Cargo.toml`、可直接运行的最小示例，以及验证命令：

```powershell
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test
cargo run
```

读者不需要先把 Rust 语言的每个角落背下来，但必须能读懂所有权、借用、`struct`、`enum`、`impl`、泛型和 `Result`。借用检查器不是拦路怪物，它在提前暴露系统之间真实的读写竞争；在游戏逻辑开始复杂之后，这种约束会比「运行十分钟后随机坏掉」便宜得多。

建议把项目拆成一个工作区或按章节建立独立目录，但每章保留一个最小可运行状态。文章示例优先使用 Bevy 原生 API；当引入第三方 crate 时，要说明其维护状态、版本约束和替代方案。教程把一串依赖塞进读者项目，然后祈祷版本解析成功，属于把认知负荷外包给未来的你。

## 学习阶段与文章地图

### 阶段 0：建立可观测的实验台

**目标：** 启动窗口、理解 `App`、插件和基本 Schedule，并学会从日志、Gizmos、实体检查中确认引擎到底做了什么。

- 文章 00：《Bevy 不是引擎黑箱：一个 `App` 如何开始运行》
- 文章 01：《插件、资源与系统：先把运行时的骨架搭出来》
- 文章 02：《调试可视化：日志、Gizmos 与最小复现》

**实验交付：** 一个可切换调试覆盖层的窗口：显示帧时间、实体计数，以及一条由 Gizmos 绘制的坐标轴。

**验收问题：** 读者能解释 `DefaultPlugins` 做了什么，能自行添加 Resource，能定位一个「系统根本没有被调度」的错误。

### 阶段 1：ECS 的数据模型

**目标：** 用组件组合建模，而不把所有状态塞进万能 `Player` 结构体或继承树。

- 文章 03：《实体没有行为：ECS 中的身份、事实与规则》
- 文章 04：《`Query` 的读取、写入与冲突：借用检查器在保护什么》
- 文章 05：《`Commands`、延迟命令与生命周期：实体何时真正出生和消失》
- 文章 06：《事件与变更检测：别每帧轮询整个宇宙》

**实验交付：** 一个「孢子群」模拟：实体按 `Velocity` 移动，生命周期耗尽后销毁，碰撞事件生成新实体。要求拆分移动、衰减、生成和清理系统。

**验收问题：** 面对查询冲突，读者能判断应使用参数集、缩小查询、引入事件，还是调整数据模型；不靠胡乱加 `Without` 让编译器闭嘴。

### 阶段 2：时间、输入与游戏循环

**目标：** 将输入采样与确定性的游戏模拟分开，理解帧时间、固定时间步和暂停。

- 文章 07：《`Update` 与 `FixedUpdate`：帧率不是游戏规则》
- 文章 08：《输入映射：从键盘按键到可测试的意图》
- 文章 09：《状态机与 Run Conditions：菜单、游戏和暂停如何隔离》

**实验交付：** 一个俯视角角色控制器：输入生成移动意图，固定时间步更新位置，`GameState` 可切换菜单、游戏、暂停。

**验收问题：** 把帧率限制改为 30 FPS 与 144 FPS 后，角色的真实移动距离保持近似一致；暂停时模拟停止，UI 动画可选择继续。

### 阶段 3：2D 空间、相机与渲染

**目标：** 看懂坐标系和渲染分层，掌握当前项目已涉及的相机、`Viewport` 与屏幕坐标转换。

- 文章 10：《2D 坐标系：窗口像素、Viewport 与世界位置》
- 文章 11：《相机跟随、缩放与边界：看见的世界由谁决定》
- 文章 12：《精灵、纹理、图层与 Z 顺序：渲染不是碰运气》
- 文章 13：《UI 与世界空间：两套坐标系，两个问题域》

**实验交付：** 一张可缩放、可拖拽的战术地图。鼠标位置要从窗口空间转换至世界空间，并用 Gizmos 同时标记原始点击和回算位置。

**验收问题：** 窗口缩放、相机缩放和受限 Viewport 同时存在时，鼠标点选仍准确。现有 `viewport2d` 示例可作为本章的起始实验。

### 阶段 4：交互、碰撞与游戏规则

**目标：** 用明确的状态和事件实现可测试的规则，避免用渲染对象偷偷承载玩法状态。

- 文章 14：《碰撞检测的最小模型：AABB、空间划分与误差边界》
- 文章 15：《事件驱动的伤害、拾取与死亡》
- 文章 16：《从一次攻击到一条规则链：组合系统而非巨型 `match`》

**实验交付：** 一个 Arena 生存原型：玩家移动、敌人追踪、投射物、碰撞伤害、掉落物与波次生成。

**验收问题：** 改变渲染实现不会改变伤害结果；可通过运行逻辑系统或单元测试验证某个实体何时死亡。

### 阶段 5：内容管线与表现层

**目标：** 理解资产异步加载、动画、声音和 UI 的生命周期，把「能玩」推进到「玩家能读懂」。

- 文章 17：《Asset Server：资源句柄、加载状态与热重载》
- 文章 18：《2D 动画状态：帧序列、状态转换与视觉反馈》
- 文章 19：《音效与 UI：反馈信息如何进入玩家感知》
- 文章 20：《本地化与配置：把文本和数值从代码里释放出来》

**实验交付：** 为 Arena 原型添加加载界面、血条、命中反馈、音效、设置页和可编辑的平衡参数。

**验收问题：** 资源尚未加载完成时没有空句柄崩溃；连续命中不会因每帧重复触发而制造声音灾难。

### 阶段 6：工程化与发布

**目标：** 将一次性演示变成可维护的软件，学会测量性能、拆分插件、保存状态，并针对目标平台打包。

- 文章 21：《插件化项目结构：按功能切分，不按文件扩散》
- 文章 22：《测试 ECS 逻辑：世界、资源、事件与确定性》
- 文章 23：《性能剖析：先测量，再谈优化》
- 文章 24：《存档、设置与发布构建》

**实验交付：** 完成一个可发布的 2D 垂直切片：主菜单、核心循环、暂停、存档、音频设置、失败重开和发布说明。

**验收问题：** 新功能以插件加入后不需要改动不相关模块；关键规则具备自动测试；release 构建可在干净环境启动。

## 每篇文章的固定骨架

系列文章需要统一的实验协议。读者面对的是不断变化的引擎 API，文章面对的是更容易自我欺骗的「我已经懂了」错觉；固定骨架就是为了压缩无效认知负荷。

1. **Hook：** 用一句反直觉判断击破常见误解。例如：「ECS 的难点从来不在 `spawn`，它在于谁拥有修改状态的权利。」
2. **省流版：** 给出 3 至 5 条机制结论和本章交付物。
3. **问题模型：** 写清状态、输入、输出、系统边界和调度位置，再展示 API。
4. **最小实现：** 代码必须独立运行，省略内容只能省略表现，不能省略因果链。
5. **故障注入：** 故意制造一个顺序、时间步、坐标系或查询冲突问题，展示症状、定位方法与修复理由。
6. **可观测性：** 添加日志、Gizmos、调试 UI 或测试之一，让读者能看到状态如何变化。
7. **练习与变体：** 提出一项受约束改动。例如将单玩家查询扩展为多实体，而不是空泛地说「尝试优化」。
8. **版本记录与延伸阅读：** 标记适用的 Bevy 版本，列出官方文档、示例和下一章链接。

代码章节应明确区分「引擎接口事实」与「架构选择」。前者需要链接官方文档或源码；后者要说明代价。把个人偏好伪装成唯一正确架构，是教程作者最廉价的权威感来源。

每章的README之外，请用同样的标准为每一章撰写一个相关度非常高的Rust语言构造与设计技术的文档：确保读者能够理解语言特性来支持使用Bevy；确保读者能够从Bevy的设计理念、使用框架中看到Rust常用的设计模式与最佳实践。这个文档请命名为Rust_in_Bevy.md。

## 引用与资料准则

Bevy 的 API 以官方文档、官方示例、迁移指南和 crate 文档为一手资料。对于 ECS、游戏循环、数据导向设计等基础理论，可补充学术资料或可靠技术文章，但不能把博客印象加工成「业界定论」。

引用翻译内容时，使用标准版头：

> 以下内容摘自 @Thoughts Memo 汉化组的译文《[文章标题]》。

原作者的观点需标出作者、作品和上下文；无法核验的引语不要使用。生成式 AI 可以用于归纳 API 差异、辅助翻译和生成实验变体，但它常把不同 Bevy 版本的接口揉成一锅文本浆糊。所有 AI 生成代码都必须通过 `cargo fmt`、`cargo clippy`、测试和实际运行验证。（确信

## 里程碑与节奏

每周安排 4 个学习单元，每个单元 60 至 90 分钟：

- 单元 A：阅读一篇文章和对应官方示例，画出数据流。
- 单元 B：从空项目重建最小实验，禁止盯着成品逐行照抄。
- 单元 C：加入一个故障变体并修复，记录编译器或运行时给出的信号。
- 单元 D：把本周机制并入持续项目，写一段自己的设计说明。

阶段 0 至阶段 2 约需 4 周，阶段 3 至阶段 4 约需 4 周，阶段 5 至阶段 6 约需 4 至 6 周。时间不是 KPI；每个阶段的验收实验无法解释时，继续追赶下一章只是在给知识缺口铺地毯。

建议维护一份学习日志，每次只记录四项：遇到的现象、对状态归属的判断、验证方式、最终结论。它比「今天学了 3 小时」更能暴露真实的认知进度。

## 完结标准

完成本路线后，读者应能独立完成一个中小型 2D 游戏原型，并能：

- 为新功能定义实体、组件、资源、事件与系统边界；
- 根据读写依赖安排系统顺序，并解释选择；
- 正确处理帧时间、固定更新、输入和状态切换；
- 在窗口、相机、Viewport、UI 与世界坐标之间定位转换错误；
- 用日志、Gizmos、测试和性能测量定位问题；
- 阅读 Bevy 新版本迁移指南，自行修复 API 演进造成的破坏。

这份能力和「背得出多少个插件名」无关。游戏引擎从来不缺启动模板，缺的是能把混乱需求压缩成清晰状态机和可验证数据流的人。

## 延伸阅读

- [Bevy 官方文档](https://bevy.org/learn/)
- [Bevy `0.19` API 文档](https://docs.rs/bevy/0.19.1/bevy/)
- [Bevy 官方示例索引](https://bevy.org/examples/)
- [Bevy 迁移指南](https://bevy.org/learn/migration-guides/)
- [Rust Book：理解所有权](https://doc.rust-lang.org/book/ch04-00-understanding-ownership.html)